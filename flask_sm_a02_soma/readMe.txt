A tag label - o prop for = tem que ser o mesmo que o id 
da tag que vc quer que ele seja o label.

Pegar dados no flask
Preciso acessar a variavel blobal request.  Importar o request.
 -ele irá trazer como dicionário todos as parametros do formulario 
    as chaves dos dicionarios será a propriedade name do formulario.

-O flask renderiza todo o html para enviar, ou seja
 se for uma pagina muito pesado pode ficar muito lenta.
 -podemos utilizar o flask como a API.

 Instalar jinja no VSCODE para ajudar na visualização do jinja

flask-sm2701